version: '3.8'
services:
  gateway:
    build: ./gateway
    ports:
      - "8000:8000"
    environment:
      - PORT=8000

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
